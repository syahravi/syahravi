name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: syahravi/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.4fb6a035-1499-4467-8456-9629481a5a59 }}
